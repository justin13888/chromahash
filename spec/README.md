# ChromaHash Format Specification

This directory defines the canonical format specification for chromahash LQIP (Low Quality Image Placeholder).

All four language implementations (Rust, TypeScript, Kotlin, Swift) MUST produce identical output for the same input and pass the same test vectors defined here.

## Status

Work in progress.
