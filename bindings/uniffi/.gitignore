# Rust crate (chromahash-uniffi)
target/
Cargo.lock
*.rs.bk

# Gradle / Kotlin build outputs for both the JVM (chromahash-jvm) and Android
# (chromahash-android) modules.
.gradle/
.kotlin/
build/

# Android binding — generated Kotlin + cross-compiled native libs regenerated by
# cargo-ndk + uniffi-bindgen; never committed.
android/src/main/jniLibs/
android/src/main/kotlin/
